function GameService() {

    // PRIVATE

    // Utility Methods (keep in constructor?)

    // Game Constructors

    // Game Data
        //player, enemy, items, etc.
        //TODO: Restructure so that all stats are nested in parent object to avoid having to store
        //individual keywords for iteration

    // PUBLIC
}